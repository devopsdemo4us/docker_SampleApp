sudo: required

services:
  - docker

before_install:
  - docker build -t devopsdemo4us/docker-react -f Dockerfile.dev .

script:
  - docker run devopsdemo4us/docker-react npm run test -- --coverage

deploy:
  provider: elasticbeanstalk
  region: ""
  app: "docker-react"
  env: "DockerReact-env"
  bucket_name: ""
  bucket_path: "docker-react"
  on:
    branch: master